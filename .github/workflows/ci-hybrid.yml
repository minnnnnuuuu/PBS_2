name: Hybrid Service Build and Push (AI Engine)

on:
  push:
    branches: [ "main" ]
    paths:
      - 'manifests/ai-engine/**'
      - 'manifests/on-prem/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 1. 코드 가져오기
      uses: actions/checkout@v4

    - name: 2. AWS 신분증 확인
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-northeast-2

    - name: 3. AWS ECR 로그인
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: 4. 하이브리드 이미지 빌드 및 전송 (Push)
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: hybrid-service
        IMAGE_TAG: ai-latest
      run: |
        # Dockerfile이 있는 경로를 컨텍스트로 지정하여 빌드
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./manifests/ai-engine
        
        # 빌드된 이미지를 ECR로 전송
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        echo "✅ AI 엔진 이미지(ai-latest) 전송 완료!"

    # [추가] 5. EKS 파드 자동 재시작 (새 이미지 반영)
    - name: 5. EKS 배포 갱신 (Rollout Restart)
      run: |
        # EKS 클러스터 접속 설정 (클러스터 이름을 확인해 주세요)
        aws eks update-kubeconfig --region ap-northeast-2 --name pbs-eks-cluster
        
        # 새로운 이미지를 가져오도록 배포(Deployment) 재시작
        kubectl rollout restart deployment hybrid-ai-service
        echo "🚀 EKS 배포 갱신 완료! 이제 새 코드가 적용됩니다."