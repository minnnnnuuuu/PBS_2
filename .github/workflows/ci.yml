name: Build and Push to ECR (Website)

on:
  push:
    branches: [ "main" ]
    # 웹 소스나 배포 설정이 바뀔 때 실행
    paths:
      - 'website/**'
      - 'manifests/eks/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 1. 코드 가져오기
      uses: actions/checkout@v4

    - name: 2. AWS 신분증 확인
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-northeast-2

    - name: 3. AWS ECR 로그인
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: 4. 웹사이트 이미지 빌드 및 전송 (Push)
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: pbs-project-repo
        IMAGE_TAG: web-latest
      run: |
        # website/ 폴더 안의 소스를 사용하여 빌드
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG website/
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        echo "✅ 웹사이트 이미지(web-latest) 전송 완료!" 

    - name: 5. EKS 웹 배포 갱신 (Rollout Restart)
      run: |
        aws eks update-kubeconfig --region ap-northeast-2 --name pbs-project-dev-cluster
        # pbs-web 배포가 새 이미지를 가져오도록 재시작
        kubectl rollout restart deployment pbs-web
        echo "🚀 웹사이트 서비스 갱신 완료!"